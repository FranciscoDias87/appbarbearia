import {Body,Controller,Delete,Get,Param,Patch,Post,UseGuards} from '@nestjs/common'; import {IsInt,IsNumber,IsOptional,IsString,Min} from 'class-validator'; import {Role} from '@prisma/client'; import {PrismaService} from '../prisma.service'; import {JwtAuthGuard} from '../auth/auth.guard'; import {RolesGuard} from '../common/guards/roles.guard'; import {Roles} from '../common/decorators/roles.decorator'; import {CurrentUser} from '../common/decorators/current-user.decorator';
class ServiceDto {@IsString() name!:string; @IsOptional() @IsString() description?:string; @IsInt() @Min(1) duration!:number; @IsNumber() @Min(0) price!:number;}
@Controller('services') @UseGuards(JwtAuthGuard,RolesGuard) export class ServicesController {constructor(private prisma:PrismaService){} @Get() list(@CurrentUser()u:any){return this.prisma.service.findMany({where:{barbershopId:u.barbershopId,isActive:true},orderBy:{name:'asc'}})} @Post() @Roles(Role.ADMIN) create(@CurrentUser()u:any,@Body()d:ServiceDto){return this.prisma.service.create({data:{...d,barbershopId:u.barbershopId}})} @Patch(':id') @Roles(Role.ADMIN) update(@CurrentUser()u:any,@Param('id')id:string,@Body()d:Partial<ServiceDto>){return this.prisma.service.updateMany({where:{id,barbershopId:u.barbershopId},data:d}).then(()=>this.prisma.service.findUnique({where:{id}}))} @Delete(':id') @Roles(Role.ADMIN) remove(@CurrentUser()u:any,@Param('id')id:string){return this.prisma.service.updateMany({where:{id,barbershopId:u.barbershopId},data:{isActive:false}})} }
