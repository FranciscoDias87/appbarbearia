'use client';
import Link from 'next/link'; import { CalendarDays, LogOut, Scissors, Shield, UserRound } from 'lucide-react'; import { useAuth } from './auth-provider';
const items = { CLIENT: [{ href: '/client', label: 'Início', icon: UserRound }, { href: '/client/agendar', label: 'Agendar', icon: CalendarDays }, { href: '/client/agendamentos', label: 'Meus horários', icon: CalendarDays }], BARBER: [{ href: '/barber', label: 'Agenda', icon: CalendarDays }, { href: '/barber/horarios', label: 'Horários de trabalho', icon: Scissors }, { href: '/barber/bloqueios', label: 'Bloqueios', icon: Shield }], ADMIN: [{ href: '/admin', label: 'Visão geral', icon: Shield }, { href: '/admin/agendamentos', label: 'Agendamentos', icon: CalendarDays }, { href: '/admin/barbeiros', label: 'Barbeiros', icon: UserRound }, { href: '/admin/servicos', label: 'Serviços', icon: Scissors }] };
export function AppShell({ children }: { children: React.ReactNode }) { const { user, signOut } = useAuth(); if (!user) return null; return <div className="min-h-screen md:flex"><aside className="bg-ink px-5 py-6 text-white md:w-64"><Link href={`/${user.role.toLowerCase()}`} className="mb-9 flex items-center gap-2 text-xl font-bold"><span className="rounded-lg bg-leather p-2"><Scissors size={19}/></span>Barbearia</Link><nav className="flex gap-2 overflow-auto md:block">{items[user.role].map(({ href, label, icon: Icon }) => <Link key={href} href={href} className="mb-1 flex items-center gap-3 whitespace-nowrap rounded-xl px-3 py-2.5 text-stone-300 hover:bg-white/10 hover:text-white"><Icon size={18}/>{label}</Link>)}</nav><button onClick={signOut} className="mt-6 flex items-center gap-3 rounded-xl px-3 py-2.5 text-stone-300 hover:bg-white/10"><LogOut size={18}/>Sair</button></aside><main className="mx-auto w-full max-w-6xl p-5 md:p-9"><header className="mb-8"><p className="text-sm text-leather">Olá, {user.name.split(' ')[0]}</p><h1 className="text-2xl font-bold">Seu espaço, no seu ritmo.</h1></header>{children}</main></div>; }
